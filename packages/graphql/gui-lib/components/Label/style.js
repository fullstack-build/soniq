import { ONE_TEXT_GRAY } from "../../constants/colors";

export const style = {
  wrapper: {
    color: ONE_TEXT_GRAY,
    fontSize: 12,
    fontWeight: 300,
    marginTop: 16,
    marginBottom: 6
  }
};